import React from "react";

const ResourceCard = () => {
  return (
    <div>
      {/* <div key={resource?._id}>
        <h1>{resource?.title}</h1>
      </div> */}
    </div>
  );
};

export default ResourceCard;
